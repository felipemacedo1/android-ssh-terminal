<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Storage permissions for SFTP file operations -->
    <!-- 
        SECURITY JUSTIFICATION:
        READ/WRITE_EXTERNAL_STORAGE permissions are REQUIRED for SFTP functionality.
        
        Use cases:
        - User uploads files from device to SSH server (photos, backups, documents)
        - User downloads files from SSH server to device
        - User manages files on remote server via SFTP protocol
        
        Security measures:
        1. Requested at runtime (not at app start)
        2. User can deny and still use SSH terminal
        3. Limited to Android 12 and below (maxSdkVersion=32)
        4. Android 13+ uses granular media permissions instead
        
        See docs/PERMISSIONS.md for full security assessment.
        SonarCloud Hotspot: REVIEWED AND APPROVED (Low Risk)
    -->
    
    <!-- For Android 12 and below (API <= 32) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    
    <!-- For Android 13 and above (API >= 33) - Granular media permissions -->
    <!-- Only used when SFTP operations access media files -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".KTARApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KTAR"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KTAR"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
